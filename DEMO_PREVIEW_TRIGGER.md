This repository was committed with a minimal file to trigger a preview deploy for the `demo-glass-component` branch.

This commit is minimal and only used to trigger the Vercel preview workflow for UI review. It contains no code changes.

If you'd prefer me not to commit or push, tell me and I will abort.
